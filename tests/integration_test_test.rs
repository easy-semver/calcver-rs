// extern crate calcver;
// extern crate git2;

// use git2::{Repository, Signature, Commit, ObjectType, Time, DiffOptions};

// #[test]
fn placeholder() {
//     let repo =  calcver::ProjectRepo {
//         path: "".to_string(),
//         commit_template: "".to_string(),
//         tag_template: "".to_string(),
//    };
   
    // assert_eq!(calcver::get_version(&repo, calcver::VersionBumpBehavior::Auto, true),"1.0.0");

    // let r = Repository::open("D:\\Workspace_V2\\src\\github.com\\easy-semver\\calcver-rs".to_string()).expect("repository should open!!!");
    // let mut revwalk = r.revwalk().expect("revwalk");
    // revwalk.set_sorting(git2::SORT_TOPOLOGICAL);
    // revwalk.push_head();

    // for commit in revwalk {
    //     let tae = r.find_commit(commit.unwrap());
    //     println!("commit {}", tae.unwrap().id().to_string());
    // }
    
}